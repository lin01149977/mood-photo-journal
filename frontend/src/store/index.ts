/**
 * Global pinia stores live here (cross-module concerns such as the current
 * user session or app-wide config). Module-local stores stay inside their own
 * `modules/<name>/store/` and must NOT be re-exported from here.
 *
 * Pinia itself is installed in `main.ts` via `app.use(createPinia())`.
 */

export {}
